{
  "name": "IP Sensor Webhook",
  "version": "1.0",
  "slug": "ip_sensor_webhook",
  "description": "Empfängt JSON Webhooks von IP-Geräten und meldet Werte per MQTT",
  "startup": "application",
  "boot": "auto",
  "homeassistant": "2021.12.0",
  "arch": ["aarch64", "amd64", "armv7"],
  "options": {},
  "schema": {},
  "ingress": true,
  "ingress_port": 5000,
  "map": ["config:rw"],
  "host_network": true,
  "image": "local/ip_sensor_webhook",
  "ports": {
    "5000/tcp": 5000
  }
}